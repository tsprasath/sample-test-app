# ═══════════════════════════════════════════════════════
# ArgoCD ApplicationSet — Auto-discovers Helm charts
# ═══════════════════════════════════════════════════════
# For each directory under infra/helm-charts/ that has a Chart.yaml,
# AND for each environment (dev/staging/prod), create an ArgoCD Application.
#
# When you add a new service:
#   1. Create infra/helm-charts/<service-name>/Chart.yaml + values*.yaml
#   2. ArgoCD auto-creates the Application — no manual YAML needed
#
# This replaces the per-service per-env YAML files in argocd-apps/{env}/.

apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: diksha-services
  namespace: argocd
spec:
  generators:
    - matrix:
        generators:
          # Generator 1: environments
          - list:
              elements:
                - env: dev
                  namespace: diksha-app-dev
                  autoSync: "true"
                - env: staging
                  namespace: diksha-app-staging
                  autoSync: "true"
                - env: prod
                  namespace: diksha-app-prod
                  autoSync: "false"
          # Generator 2: auto-discover services from repo directory structure
          - git:
              repoURL: https://github.com/tsprasath/ai-devops.git
              revision: main
              directories:
                - path: infra/helm-charts/*
  template:
    metadata:
      name: "{{path.basename}}-{{env}}"
      namespace: argocd
      labels:
        app.kubernetes.io/name: "{{path.basename}}"
        app.kubernetes.io/part-of: diksha-dev
        diksha.dev/environment: "{{env}}"
      finalizers:
        - resources-finalizer.argocd.argoproj.io
    spec:
      project: default
      source:
        repoURL: https://github.com/tsprasath/ai-devops.git
        targetRevision: main
        path: "{{path}}"
        helm:
          valueFiles:
            - values.yaml
            - "values-{{env}}.yaml"
      destination:
        server: https://kubernetes.default.svc
        namespace: "{{namespace}}"
      syncPolicy:
        syncOptions:
          - CreateNamespace=false
          - PrunePropagationPolicy=foreground
          - ServerSideApply=true
        retry:
          limit: 3
          backoff:
            duration: 5s
            factor: 2
            maxDuration: 3m
